@model personapi_dotne.Models.Entities.Profesion

@{
    ViewData["Title"] = "Crear Profesión";
}

<h1 class="mb-4 text-primary">Crear Profesión</h1>

<div class="card shadow p-4">
    <form asp-action="Create">
        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

        <div class="mb-3">
            <label asp-for="Nom" class="form-label">Nombre</label>
            <input asp-for="Nom" class="form-control" />
            <span asp-validation-for="Nom" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Des" class="form-label">Descripción</label>
            <textarea asp-for="Des" class="form-control" rows="4"></textarea>
            <span asp-validation-for="Des" class="text-danger"></span>
        </div>

        <div class="d-flex justify-content-between mt-4">
            <button type="submit" class="btn btn-primary">Guardar</button>
            <a asp-action="Index" class="btn btn-secondary">Volver</a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
